import { ArrowUpRight, BarChart3 } from "lucide-react";
import { site } from "@/config/site";
import { CopyContract, ExternalLink, Reveal, SectionHeading } from "./ui";
export function TokenInfo() { const items = [["Name", site.name], ["Ticker", site.ticker], ["Network", site.network], ["Total Supply", site.supply]]; return <section id="token-info" className="section shell"><SectionHeading eyebrow="THE ESSENTIALS" title="Token Info" center/><div className="token-grid">{items.map(([a,b]) => <Reveal className="info-card" key={a}><span>{a}</span><strong>{b}</strong></Reveal>)}</div><Reveal className="contract-card"><div><span>CONTRACT ADDRESS</span><CopyContract compact/></div><ExternalLink href={site.links.dex} className="button">View on RadarDEX <ArrowUpRight size={18}/></ExternalLink></Reveal><Reveal className="market-card"><BarChart3/><div><b>Live market data</b><p>For current market information, visit the listed pair on RadarDEX.</p></div><ExternalLink href={site.links.dex}>View live market data on RadarDEX <ArrowUpRight size={16}/></ExternalLink></Reveal></section>; }
